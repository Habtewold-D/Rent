import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand colors
  static const Color darkCyan = Color(0xFF008B8B);
  static const Color cyanAccent = Color(0xFF00A6A6);

  // Dark scheme
  static const Color darkBg = Color(0xFF0F1115);
  static const Color darkSurface = Color(0xFF171A21);

  // Light scheme
  static const Color lightBg = Color(0xFFF7FAFC);
  static const Color lightSurface = Color(0xFFFFFFFF);
}
